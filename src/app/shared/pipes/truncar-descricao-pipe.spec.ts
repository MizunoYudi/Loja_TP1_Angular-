import { TruncarDescricaoPipe } from './truncar-descricao-pipe';

describe('TruncarDescricaoPipe', () => {
  it('create an instance', () => {
    const pipe = new TruncarDescricaoPipe();
    expect(pipe).toBeTruthy();
  });
});
